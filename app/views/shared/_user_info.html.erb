<%= link_to gravatar_for(current_user, size: 52), display_user_path(current_user) %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "View my profile", display_user_path(current_user) %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "micropost") %>
</span>